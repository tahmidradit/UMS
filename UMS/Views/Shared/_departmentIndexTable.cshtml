@model IEnumerable<Department>
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager

@if (SignInManager.IsSignedIn(User))
{
    <div>
    @if (Model.Count() > 0)
    {
        <div class="mt-2">
            <table id="" class="table table-hover table-bordered table-sm text-center" style="background-color:lightskyblue" width="100%">
                    <thead class="table table-primary">
                        <tr>
                            <th>@Html.DisplayNameFor(m => m.Id)</th>
                            <th>@Html.DisplayNameFor(m => m.Name)</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var items in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(m => items.Id)</td>
                                <td>@Html.DisplayFor(m => items.Name)</td>
                                <td>
                                    <a asp-controller="Department" asp-action="Edit" asp-route-id="@items.Id"  class="btn btn-dark text-white btn-sm"><i class="fas fa-edit"></i> &nbsp; Edit</a>
                                    <a class="btn btn-danger text-white btn-sm" asp-action="Delete" asp-controller="Department" asp-route-id="@items.Id"><i class="fas fa-trash-alt"></i> &nbsp; Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
            </table>
        </div>
    }
    else
    {
        <br />
        <div class="alert alert-danger text-danger" role="alert">No department records available right now !</div> 
    }
    </div>
}
else
{
    <br />
        <div class="alert alert-danger text-danger" role="alert">Sign in First to continue !</div> 
}


